# Template seçim ağırlıkları (tam sayı, göreli).
# video_factory.sh bu dosyayı okur. Listede OLMAYAN template varsayılan 25 alır.
# Yeni varyant eklersen: templates/'e .tmpl koy; ağırlık istersen buraya satır ekle,
# istemezsen dokunma (varsayılan 25 ile havuza girer).
hook=50
quote=25
stat=25
